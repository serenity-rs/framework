initSidebarItems({"fn":[["content","Returns the content of the message after parsing a prefix. The content is defined as the substring of the message after the prefix. If the `Configuration::no_dm_prefix` option is enabled, the content is the whole message."],["dynamic_prefix","Parses a prefix from the message dynamically using the `Configuration::dynamic_prefix` hook."],["mention","Parses a mention from the message. A mention is defined as text starting with `<@`, which may be followed by `!`, proceeded by a user id, and ended by a `>`."],["static_prefix","Parses a prefix from the message statically from a list of prefixes."]]});